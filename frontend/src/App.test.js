import { render, screen } from '@testing-library/react';
import App from './App';
import { get_bro, get_tables} from "./api/api-wrapper";('./api/api-wrapper');

test('renders learn react link', () => {
 

    // get_tables().then((data) => {
    //     console.log(data)
    // })

    // get_bro(10).then((data) => {
    //     console.log(data)
    // })

});
